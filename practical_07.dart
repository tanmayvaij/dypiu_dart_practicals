// Replace a part of a string (using built in string class)

void main() {

  String name = "Tomamay";

  // replaceRange( start (include), end (exclude), string )
  print(name.replaceRange(1, 4, "an"));

}
