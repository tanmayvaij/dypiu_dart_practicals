// Remove the last character and print out the result string.

void main() {

  String str = "This is a sample string";

  // substring( start (include), end (exclude), string )
  print(str.substring(0, str.length-1));

}
